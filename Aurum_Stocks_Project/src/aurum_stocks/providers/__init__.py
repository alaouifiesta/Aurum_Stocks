"""Provider interfaces + mock implementations (news / halt / data-quality).
Where real SIP/vendor data is required, the system depends on these ABCs, never a vendor."""
from . import providers  # noqa: F401
