"""RESERVED for Priority #3 scanner implementations. Empty by design.

Only the scanner *registry* (provenance) exists in Priority #2, under
aurum_stocks.registries.scanner_registry. No scanner logic is built here yet."""
